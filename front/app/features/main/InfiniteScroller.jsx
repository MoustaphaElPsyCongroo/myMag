// import { useEffect, useRef } from "react"

// const InfiniteScroller = ({ children, loading, loadNext }) => {
// 	const scrollListener = useRef(loadNext);

// 	useEffect(() => {
// 		scrollListener.current = loadNext;
// 	}, [loadNext]):

// 	const onScroll = () => {

// 	}
// }
